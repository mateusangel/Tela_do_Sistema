import { StyledCadastrar } from "./StyledCadastrar";

// eslint-disable-next-line react/prop-types
export const Cadastrar = ({ children }) => {
  return (
    <>
      <StyledCadastrar>{children}</StyledCadastrar>
    </>
  );
};
